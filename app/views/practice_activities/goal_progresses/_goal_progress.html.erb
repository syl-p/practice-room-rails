<div id="<%= dom_id(progress) %>" class="p-4 hover:bg-gray-50">
  <p class="text-sm text-muted-foreground mb-1">
    <%= progress.value %> <%= goal.unit %>
  </p>
  <p class="text-xs text-muted-foreground">
    <%= l(progress.created_at, format: :short) %>
  </p>
</div>