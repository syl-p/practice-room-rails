<div id="flash-zone" class="fixed z-[100] bottom-6 left-6 flex flex-col space-y-6">
  <%= turbo_frame_tag "flash" do %>
    <% flash.each do |type, message| %>
      <%= render partial: "shared/flash", locals: {type:, message:} %>
    <% end %>
  <% end %>
</div>