<% content_for :title, "New practice" %>

<section class="container py-20">
  <%= render BreadcrumbComponent.new([{label: 'Nouvelle pratique', path: '#'}]) %>
  <h1 class="font-bold text-4xl mb-6">Nouvelle pratique</h1>
  <%= render "form", practice: @practice %>
</section>
