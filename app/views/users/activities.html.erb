<%= turbo_frame_tag :user_activities_preview do %>
  <ul class="mb-6">
    <% @activities.each do |activity| %>
      <div class="bg-secondary border">
        <h4 class="h3"><%= link_to activity.title, activity %></h4>
      </div>
    <% end %>
  </ul>
<% end %>