<section class="container">
  <header class="pt-20 mb-10">
    <h1 class="text-6xl font-bold uppercase">
      Bonjour, qu'allons nous travailler ?
    </h1>
  </header>
  <%= render partial: "activities/filters" %>
  <%= turbo_frame_tag "activities" do %>
    <%= render partial: "activities/card", collection: @activities, as: :activity, locals: {classes: 'mb-3'} %>
  <% end %>
</section>
