<%= turbo_frame_tag :remote_modal do %>
	<dialog data-controller="remote-modal" class="backdrop:bg-background/80 backdrop:backdrop-blur-sm w-full md:max-w-[600px] lg:min-w-[900px] max-w-2xl gap-4 border bg-background shadow-lg duration-200 rounded-md">
		<div class="p-6">
  		<%= render partial: "activities/form" %>
		</div>
	</dialog>
<% end %>
