<% if notifications.any? %>
	<ul class="divide-y divide-border max-h-96 overflow-y-auto">
		<% notifications.each do |notification| %>
			<li><%= render notification %></li>
		<% end %>
	</ul>
<% else %>
	<%= render partial: "notifications/empty_state" %>
<% end %>