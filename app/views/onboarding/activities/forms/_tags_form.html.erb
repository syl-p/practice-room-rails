<%# Locals: form_builder (f), activity %>
<%= render partial: "tags/tags_selector",
           locals: { preselected_tags: activity.tags,
                     field_name: "onboarding_activity_tags_step[labels]" } %>